DATABASE_URL=

PORT=
DEFAULT_PAGE_SIZE=
NODE_ENV=
LOG_LEVEL=
KEEP_ALIVE_TIMEOUT_MS=
HEADERS_TIMEOUT_MS=
REQUEST_TIMEOUT_MS=
SHUTDOWN_TIMEOUT_MS=

# Frontend origin allowed by CORS in production (leave unset in dev to allow all)
FRONTEND_ORIGIN=

# Cloudinary — used to store career applicant resumes
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Email (SMTP) — used to notify HR of new career applications
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
HR_NOTIFICATION_EMAIL=
